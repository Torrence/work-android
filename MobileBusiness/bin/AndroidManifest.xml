<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.led.android" android:versionCode="1" android:versionName="1.0">
	<application android:icon="@drawable/icon" android:label="@string/app_name">
		<activity android:name=".Login" android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<!-- 注册activity -->
		<activity android:name="MainActivity"></activity>
		<activity android:name="SearchOne"></activity>
		<!-- 配置configChanges属性确保横屏不刷新画面数据 -->
		<activity android:name="SearchAll" android:configChanges="orientation|keyboardHidden"></activity>
		<activity android:name="ViewOne"></activity>
	</application>
	<!-- 应用程序联网权限 -->
	<uses-permission android:name="android.permission.INTERNET" />
</manifest> 